Host *
    ForwardAgent no
    ForwardX11 no
    PermitLocalCommand no
    # Avoid keys being sent to any SSH server we connect to (ex: whoami.filippo.io)
    PubkeyAuthentication no
    IdentitiesOnly yes
    AddKeysToAgent no
